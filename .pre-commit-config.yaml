repos:
  - repo: local
    hooks:
      - id: ci-quality-gates
        name: Central quality gates
        entry: bash scripts/run-ci-quality-gates.sh --fix --stage
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
